name: Bootstrap Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # Checks out repository under $GITHUB_WORKSPACE
      - uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Add repo to Python PATH
        run: |
          # Ensure this repo is on the $PYTHONPATH, which Python uses to
          # determine where modules live
          export PYTHONPATH=$PYTHONPATH:~/.dotfiles

      - name: Bootstrap
        run: |
          # Ensure this repo is on the $PYTHONPATH, which Python uses to
          # determine where modules live
          export PYTHONPATH=$PYTHONPATH:~/.dotfiles
          python builder/bootstrap.py

      - name: Run Python tests
        run: python -m unittest discover -p '*test.py'

      - name: Run Rust tests
        run: cargo test
